# Always prefer setuptools over distutils
from codecs import open
from os import path
import os
from setuptools import setup, find_packages

# To use a consistent encoding
here = path.abspath(path.dirname(__file__))

setup(
    name='alarm_analytics'
)
